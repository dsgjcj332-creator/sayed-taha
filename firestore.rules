rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /portfolio/{docId} {
      // Public edit mode for this portfolio admin panel.
      // Deploy this rule so App.tsx can create/update portfolio/main from the browser.
      allow read, create, update: if true;
      allow delete: if false;
    }
  }
}
